services:
  postgres:
    image: postgres:16
    container_name: beautymatch
    restart: always

    environment:
      POSTGRES_USER: joaos
      POSTGRES_PASSWORD: Jr300904
      POSTGRES_DB: tccdb

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
